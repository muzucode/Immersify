<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kanjicardsgo">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KanjiCardsGo">
        <activity android:name=".track_route.AddTrackActivity"></activity>
        <activity android:name=".track_route.SelectTrackActivity" />
        <activity android:name=".signin.SignInActivity" />
        <activity android:name=".signup.SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".managedecks.createdeck.CreateDeckNameActivity" />
        <activity android:name=".managedecks.createdeck.CreateDeckActivity" />
        <activity android:name=".managedecks.ManageDecksActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".StudyActivity" />
        <activity android:name=".VocabActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SelectDecksActivity" />
    </application>

</manifest>